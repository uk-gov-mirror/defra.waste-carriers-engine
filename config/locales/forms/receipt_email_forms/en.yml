en:
  waste_carriers_engine:
    receipt_email_forms:
      new:
        title: Where should we send payment receipts?
        heading: Where should we send payment receipts?
        paragraph_1: We will send all receipts for payment to this address once the payment has been processed.
        paragraph_2: If you leave this blank, we will send receipts to the contact email, %{contact_email}
        error_heading: Something is wrong
        receipt_email_label: Email address
        next_button: Continue
  activemodel:
    errors:
      models:
        waste_carriers_engine/receipt_email_form:
          attributes:
            reg_identifier:
              invalid_format: "The registration ID is not in a valid format"
              no_registration: "There is no registration matching this ID"
              renewal_in_progress: "This renewal is already in progress"
