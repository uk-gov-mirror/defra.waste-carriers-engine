<%= render("shared/back", back_path: back_registration_number_forms_path(@registration_number_form.reg_identifier)) %>

<% if @registration_number_form.errors.any? %>

  <h1 class="heading-large"><%= t(".error_heading") %></h1>

  <% @registration_number_form.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>

<% else %>

  <%= form_for(@registration_number_form) do |f| %>
    <%= render("shared/errors", object: @registration_number_form) %>

    <h1 class="heading-large"><%= t(".heading") %></h1>

    <%= f.hidden_field :reg_identifier, value: @registration_number_form.reg_identifier %>
    <div class="form-group">
      <%= f.submit t(".next_button"), class: "button" %>
    </div>
  <% end %>

<% end %>
