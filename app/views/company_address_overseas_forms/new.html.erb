<%= render("shared/back", back_path: back_company_address_overseas_forms_path(@company_address_overseas_form.reg_identifier)) %>

<% if @company_address_overseas_form.errors.any? %>

  <h1 class="heading-large"><%= t(".error_heading") %></h1>

  <% @company_address_overseas_form.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>

<% else %>

  <%= form_for(@company_address_overseas_form) do |f| %>
    <%= render("shared/errors", object: @company_address_overseas_form) %>

    <h1 class="heading-large"><%= t(".heading") %></h1>

    <%= f.hidden_field :reg_identifier, value: @company_address_overseas_form.reg_identifier %>
    <div class="form-group">
      <%= f.submit t(".next_button"), class: "button" %>
    </div>
  <% end %>

<% end %>
