<%= render("shared/back", back_path: back_contact_phone_forms_path(@contact_phone_form.reg_identifier)) %>

<% if @contact_phone_form.errors.any? %>

  <h1 class="heading-large"><%= t(".error_heading") %></h1>

  <% @contact_phone_form.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>

<% else %>

  <%= form_for(@contact_phone_form) do |f| %>
    <%= render("shared/errors", object: @contact_phone_form) %>

    <h1 class="heading-large"><%= t(".heading") %></h1>

    <%= f.hidden_field :reg_identifier, value: @contact_phone_form.reg_identifier %>
    <div class="form-group">
      <%= f.submit t(".next_button"), class: "button" %>
    </div>
  <% end %>

<% end %>
